/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: 'export',
  images: {
    unoptimized: false,
  },
   
  // reactStrictMode: true,
  // swcMinify: true,
}

module.exports = nextConfig
